 <nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-4">
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
        
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
                <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item ">
        <a class="nav-link" href="/class/list">List Classes</a>
      </li>
      <li class="nav-item ">
        <a class="nav-link" href="/class/add">Add Class</a>
      </li>
    </ul>
    <form class="form-inline my-2 my-lg-0" method="post" action="/class/list">
              <input class="form-control mr-sm-2" type="search" name="search" placeholder="Search" aria-label="Search">
              <button class="btn btn-success my-2 my-sm-0" type="submit">Search</>
            </form>
          </div>
          </div>
</nav>

<div class="container">
<h1 class="display-4">List</h1>
<table class="table table-striped">
    <thead>
        <tr>
            <th>#</th>
            <th>Name</th>
            <th>Group</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <% classes.forEach(function(classes){ %>
            <tr>
                <td><%= classes.id %></td>
                <td><%= classes.name %></td>
                <td><%= classes.group %></td>
                <td>
                    <a href="/class/edit/<%= classes.id %>" class="btn btn-primary">Edit</a>
                    <form class="d-inline" action="/class/delete/<%= classes.id %>" method="post">
                        <input type="submit" value="Delete" class="btn btn-danger">
                    </form>
                </td>
            </tr>
        <% }) %>
    </tbody>
</table>
</div>